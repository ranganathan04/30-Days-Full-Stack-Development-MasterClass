REACT HOOKS: (IN FUNCTIONAL COMPONENT)

REACT HOOK IS FUNCTION

WHICH IS SIMPLE AND BETTER THEN THE CLASS COMPONENT 

       USESTATE -- BUTTON CLICK --
       USEEFFECT -- AFTER USING USESTATE

---WITHOUT THIS TWO FUNCTION WE CANT CREATE REACT APPLICATION

------------------------------------------------------------------------------------------------------------

STEP: 1 NEW FOLDER -- 
IN ADD BAR TYPE CMD -- 
NPX CREATE-REACT-APP CLIENT--
HAPPY HACKING 

STEP: 2 CD CLIENT--
NPM START

STEP: 3 IN REACT FOLDER--
ADD CMD--
CODE .

------------------------------------------------------------------------------------------------------------------------
IN SRC 
CREATE FOLDER-- COMPONENTS-- FORM.JSX

RFCE

BEFORE FUN RETURN

CONST [COUNT(VARIABLE),SETCOUNT(METHOD)] = USESTATE(0);

             INSIDE DIV 

 <H1> COUNT = {COUNT} </H1>
 <BUTTON ONCLICK{()=> 
 (SET COUNT(COUNT+1))
 }> CLICK </BUTTON>

 <CHILED VALUE={COUNT}/>



NEW FILE IN COMPONENT FOLDER--

CHILED.JSX

RFCH

<H1> VALUE GET FROM FORM {VALUE}


--------------------------------------------------------------------------------






RFCE

BEFORE FUN RETURN

CONST [USERNAME,SETUSERNAME]=USESTATE("");
CONST [USERNUMBER,SETUSERNUMBER]=USESTATE("");
CONST [VALUE,SETVALUE]=USESTATE(FALSE);


USEEFFECT(()=>{
IF(USERNAME !=="" && USERNUMBER !==""){
SETVALUE(TRUE);
}
ELSE{
SETVALUE(FALSE);
}
});

CONST UPDATE = ()=>{
IF(VALUE){
ALERT("FORM SUBMITTED");
}
ELSE
{
ALTER("ENTER NAME");
}
}

AFTER RETURN

             INSIDE DIV
<FORM ONSUBMITT={}>
<INPUT TYPE="TEXT" ONCHANGE={(E)=>(SETUSERNAME(E.TARGET.VALUE))}/>
<INPUT TYPE="NUMBER" ONCHAGE={(E)}=>(SETUSERNUMBER(E.TARGET.VALUE/>
<INPUT TYPE="SUBMIT" />